import http from 'blocks/http';
import standartSchema from './schema-standart';

const state = {
  schemas: {
    standart: standartSchema
  }
};

const getters = {};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
